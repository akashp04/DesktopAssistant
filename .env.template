# TODO
# Copy this file to .env and update the values as needed

# API Settings
APP_NAME="Desktop Assistant API"
APP_VERSION="1.0.0"
APP_DESCRIPTION="API for managing and querying desktop assistant embeddings and vectors."

# Server Settings  
HOST="0.0.0.0"
PORT=8000
RELOAD=true
LOG_LEVEL="info"

# Qdrant Settings
QDRANT_HOST="localhost"
QDRANT_PORT=6333
# QDRANT_PATH="./qdrant_storage"  # Uncomment to use embedded mode instead of server
COLLECTION_NAME="documents"

# Embedding Settings
EMBEDDING_MODEL="BAAI/bge-small-en-v1.5"
VECTOR_SIZE=384

# Processing Settings
DEFAULT_CHUNK_SIZE=400
DEFAULT_OVERLAP=100
MAX_WORKERS=3

# Search Settings
DEFAULT_TOP_K=5
MAX_TOP_K=100
DEFAULT_SCORE_THRESHOLD=0.0

# Hybrid Search Settings
RRF_K=60
DEFAULT_SEMANTIC_WEIGHT=1.0
DEFAULT_KEYWORD_WEIGHT=1.0
DEFAULT_EXACT_WEIGHT=2.0